

# Empty Redemption History — Simplify

## Change
In `src/components/team/RedemptionHistory.tsx` (lines 120–125), remove the "No redemption history" `<h3>` header and keep only the message:

> You haven't redeemed any gift cards yet.

(Drop the "Check out the gift card shop to get started!" sentence too, per the request to "just say" the first line.)

The empty-state card stays with its centered layout and existing styling.

## Files modified
- `src/components/team/RedemptionHistory.tsx`

