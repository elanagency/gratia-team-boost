

# Slack Header Color + Heading Font Weight Fix

## Changes

### 1. `src/components/SlackFeedSection.tsx`
- **Slack header color**: Change background from `#F8F7FC` to `#350D36` (Slack's actual dark purple). Update text and icon colors to white.
- **Heading font weight**: Change `font-bold` to `font-semibold` on the h2 (line 113), matching Figma's `font-weight: 600` and `font-size: 36px`.

### 2. `src/components/RecognitionDemo.tsx`
- **Heading font weight**: Change `font-bold` to `font-semibold` on the h2 "Recognition that becomes part of how your team works" (line 74), same treatment.

