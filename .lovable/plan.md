

## Speed up carousel animation timing

### Change in `src/components/hero/RecognitionCarousel.tsx`

- Change auto-cycle interval from `6000ms` to `5000ms` (was 4000ms before, 6000ms now)
- Change transition duration from `1s` to `0.8s` (was 0.6s before, 1s now)

