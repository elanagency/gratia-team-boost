

# Add New Logo to Header

## Change
1. Copy the uploaded logo to `public/lovable-uploads/grattia-logo-new.png`
2. Update `src/components/Header.tsx` line 23 to reference the new logo image

Single file edit — swap the `src` on the existing `<img>` tag.

